'use client';

interface AddCategoryProps {
  categories: string[];
}

export default function AddCategory({ categories }: AddCategoryProps) {
  return (
    <div>
      {/* Bỏ phần hiển thị danh mục hiện có */}
    </div>
  );
}
